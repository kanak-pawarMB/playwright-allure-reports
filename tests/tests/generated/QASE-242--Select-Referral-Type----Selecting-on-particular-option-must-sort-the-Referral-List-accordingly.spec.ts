import { test, expect } from '@playwright/test';

// QASE: QASE-242 (242)
// Title: "Select Referral Type" > Selecting on particular option must sort the Referral List accordingly

test(`QASE-242 - "Select Referral Type" > Selecting on particular option must sort the Referral List accordingly`, async ({ page }) => {
  // TODO: implement steps from QASE case 242
  // This is a generated stub.
});
